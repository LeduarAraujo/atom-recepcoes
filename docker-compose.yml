version: '3.8'

services:
  atom-recepcoes:
    container_name: atom-recepcoes
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - app-network
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: 'jdbc:postgresql://ep-weathered-salad-a5vgz2iq-pooler.us-east-2.aws.neon.tech:5432/neondb'
      SPRING_DATASOURCE_USERNAME: 'neondb_owner'
      SPRING_DATASOURCE_PASSWORD: 'npg_8wKSN7HUXuOF'

networks:
  app-network:
    driver: bridge